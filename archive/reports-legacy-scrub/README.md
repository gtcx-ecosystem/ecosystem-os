# reports-legacy-scrub

Former `platforms/ecosystem/reports/`.
Bridge owns Extra L0 `reports/`; this is a legacy audit scrub residual (DOMAIN-INFRA-077).
Note: `platforms/ecosystem/` is parent-gitignored (nested .git Class A) — SoR lives in vocabulary + REPO-LAYOUT.
